server:
  domain: ${DOMAIN:-fjrg2007.com}
  token: ${AUTH_TOKEN}                      # From .env (optional for public server)
  # tcpPortMin: 10000                       # TCP tunnel port range (optional)
  # tcpPortMax: 20000
# OpenTunnel Server Configuration
# Copy this file to .env and modify as needed

# =============================================================================
# REQUIRED SETTINGS
# =============================================================================

# Your domain (e.g., tunnel.yourdomain.com)
# Tunnels will be available at: https://<subdomain>.op.<DOMAIN>
DOMAIN=op.yourdomain.com

# =============================================================================
# OPTIONAL SETTINGS
# =============================================================================

# Base path for subdomains (default: op)
# Example: with BASE_PATH=op, tunnels are at *.op.yourdomain.com
BASE_PATH=op

# Authentication tokens (comma-separated)
# If set, clients must provide one of these tokens to connect
# Leave empty for open access
AUTH_TOKENS=

# =============================================================================
# SSL/TLS CERTIFICATES
# =============================================================================

# Let's Encrypt email for certificate notifications
LETSENCRYPT_EMAIL=admin@yourdomain.com

# Use Let's Encrypt production (true) or staging (false)
# Use staging first to test, then switch to production
LETSENCRYPT_PRODUCTION=false

# Cloudflare API token for DNS-01 challenge (required for wildcard certs)
# Get it from: https://dash.cloudflare.com/profile/api-tokens
# Required permissions: Zone:DNS:Edit
CLOUDFLARE_TOKEN=

# =============================================================================
# ADVANCED SETTINGS
# =============================================================================

# Server port (default: 443)
PORT=443

# Bind host (default: 0.0.0.0)
HOST=0.0.0.0

# TCP tunnel port range
TCP_PORT_MIN=10000
TCP_PORT_MAX=20000
